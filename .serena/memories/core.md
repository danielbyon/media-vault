# Project Core

- Repository: `media-vault`; GitHub remote `danielbyon/media-vault`.
- Current checkout is a minimal Swift/Xcode-oriented scaffold: only `.gitignore`, `LICENSE`, and Serena/CodeGraph configuration are tracked; no application source, package manifest, or Xcode project is present.
- Serena project configuration is `.serena/project.yml`; CodeGraph metadata is under `.codegraph/`.
- Project-wide technical details: `mem:tech_stack`.
- Darwin-specific command guidance: `mem:suggested_commands`.
- Repository conventions: `mem:conventions`.
- Completion checks: `mem:task_completion`.