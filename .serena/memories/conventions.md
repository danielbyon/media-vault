# Repository Conventions

- The existing ignore file follows the standard Swift/Xcode layout and excludes user settings, build products, DerivedData, Swift build output, and app packaging artifacts.
- Serena's per-project override `.serena/project.local.yml` is intentionally ignored.
- No source-level naming, architecture, documentation, or testing conventions are established; derive and record them when implementation arrives.