# Suggested Commands

- Repository status: `git status --short --branch`.
- Inspect tracked contents: `git ls-tree -r --name-only HEAD`.
- No project-specific build, test, lint, or run command is established because the repository currently has no source or build manifest.
- Before selecting Swift commands, inspect for a newly added `Package.swift`, `*.xcodeproj`, or `*.xcworkspace`.