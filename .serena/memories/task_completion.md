# Task Completion

- For the current scaffold, validate repository cleanliness/scope with `git status --short --branch` and inspect the resulting diff when changes are made.
- No build, test, lint, or format gate can run until Swift source and a build manifest/project are added.
- Once a build system exists, update this memory with its exact completion commands.